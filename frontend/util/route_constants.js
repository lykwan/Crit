const EVENTS = {
  title: 'Events',
  route: '/events'
};

const GROUPS = {
  title: 'Groups',
  route: '/groups'
};

const PROFILE = {
  title: 'Profile',
  route: '/profile'
};

const RouteConstants = Object.freeze({
  EVENTS: EVENTS,
  GROUPS: GROUPS,
  PROFILE: PROFILE
});

export default RouteConstants;
